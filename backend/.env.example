# 本地演示使用 SQLite。切换 MySQL 时改 prisma/schema.prisma 的 provider 为本文件的 URL。
# SQLite:
DATABASE_URL="file:./dev.db"
# MySQL 示例（后续）：
# DATABASE_URL="mysql://user:password@127.0.0.1:3306/export_risk_guard"
PORT=3000
# 风险雷达「生成示例风险 / 清除示例」。演示环境默认开启，设为 0 关闭。
# RISK_SAMPLE_ENABLED=1
